- const isNotProfi = (percentScored < profiLevel);
if isNotProfi
  <p>#{levelExplanation.profi} и поэтому профессиональный уровень владения темой, по мнению автора, это минимум #{profiLevel}%.</p>

- const isNotExpert = (percentScored > profiLevel) && (percentScored < expertLevel);
if isNotExpert
  <p>#{levelExplanation.expert} и поэтому экспертным уровнем владения темой, по мнению автора, стоит считать минимум #{expertLevel}%.</p>
